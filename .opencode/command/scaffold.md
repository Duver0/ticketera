---
description: Genera el esqueleto de una nueva funcionalidad de ticket (módulo NestJS + pantalla Next + tests) siguiendo el stack y convenciones del proyecto.
agent: team-leader
---

# Scaffold de funcionalidad

Crea el andamiaje de una nueva funcionalidad para ticketera, siguiendo el stack
fijado (NestJS + Next.js + Prisma + Auth.js) y las convenciones de AGENTS.md.

Funcionalidad solicitada: $ARGUMENTS

Procedimiento sugerido (delega a los subagentes correctos vía el orquestador):
1. project-manager: define historias y criterios de aceptación.
2. team-leader: define contrato API y, si aplica, estado del patrón State.
3. backend: módulo NestJS + Prisma (si hay entidad nueva).
4. frontend: página/componente Next.js.
5. qa: pruebas de la funcionalidad.

No escribas todo tú; produce el plan y delega. Devuelve la lista de archivos creados/planeados.
